name: voice-ai-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - portaudio
  - ipykernel
  - pip:
      - speechrecognition
      - google-generativeai
      - pyaudio
      - python-dotenv
      - pyttsx3
      - pywin32
